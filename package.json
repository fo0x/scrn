{
  "name": "smartshot-web",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean": "rm -f tsconfig*.tsbuildinfo && rm -rf dist .vite",
    "build": "npm run clean && npm run typecheck && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.app.json --pretty false",
    "prebuild": "node scripts/ci-doctor.mjs"
  },
  "dependencies": {
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^5.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.8.2",
    "vite": "^7.0.0",
    "serve": "^14.2.4"
  }
}
